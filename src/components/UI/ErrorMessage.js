const ErrorMessage = (props) => {
  return (
    <div className="error-message">
      <p>
        Calm down your horses!! 🙈 You need to paste some code before hitting
        Convert!
      </p>
    </div>
  );
};

export default ErrorMessage;
